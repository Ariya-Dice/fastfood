import React from 'react';

/**
 * کامپوننت LoadingSpinner برگرلند
 * نمایش spinner لودینگ با استایل مشابه برگرلند
 */
const LoadingSpinner: React.FC = () => {
  return (
    <div className="loading-spinner"></div>
  );
};

export default LoadingSpinner;